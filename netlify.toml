[build]
  base = "whisperwind"
  publish = "build"
  command = "npm run build"

[functions]
  directory = "whisperwind-backend/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
